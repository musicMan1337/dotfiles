# ============================================
# Ghostty Terminal - Complete Configuration
# ============================================
# File: ~/.config/ghostty/config
# Reload: Cmd+Shift+, (macOS)
# Debug keys: Cmd+Shift+I (Terminal Inspector)
# View options: ghostty +show-config --default --docs

# --- Typography ---
font-family = JetBrainsMonoNerdFont
font-size = 14
font-thicken = true
font-thicken-strength = 180
adjust-cell-height = 2

# --- Theme and Colors ---
# Catppuccin with automatic light/dark switching
theme = light:Catppuccin Latte,dark:Catppuccin Mocha
minimum-contrast = 1.1
window-colorspace = display-p3

# --- Window and Appearance ---
background-opacity = 0.9
background-blur = 20
macos-titlebar-style = transparent
window-padding-x = 10
window-padding-y = 8
window-padding-color = extend
window-padding-balance = true
window-save-state = always
window-theme = auto
window-step-resize = true
window-subtitle = working-directory

# --- macOS ---
macos-option-as-alt = left

# --- Cursor ---
cursor-style = bar
cursor-style-blink = true

# --- Mouse ---
mouse-hide-while-typing = true
copy-on-select = clipboard

# --- Quick Terminal (Quake-style dropdown) ---
quick-terminal-position = top
quick-terminal-screen = mouse
quick-terminal-autohide = true
quick-terminal-animation-duration = 0.15

# --- Splits ---
unfocused-split-opacity = 0.85
confirm-close-surface = true

# --- Security ---
clipboard-paste-protection = true
clipboard-paste-bracketed-safe = true
clipboard-trim-trailing-spaces = true
clipboard-read = ask

# --- Shell Integration ---
shell-integration = detect
shell-integration-features = no-cursor,sudo,no-title

# --- Notifications ---
notify-on-command-finish = unfocused
notify-on-command-finish-after = 10s

# --- Bell ---
bell-features = title

# --- Performance ---
scrollback-limit = 25000000

# --- Keybindings ---

# Tabs
keybind = cmd+t=new_tab
keybind = cmd+shift+left=previous_tab
keybind = cmd+shift+right=next_tab
keybind = cmd+w=close_surface

# Splits
keybind = cmd+d=new_split:right
keybind = cmd+shift+d=new_split:down
keybind = cmd+alt+left=goto_split:left
keybind = cmd+alt+right=goto_split:right
keybind = cmd+alt+up=goto_split:top
keybind = cmd+alt+down=goto_split:bottom
keybind = cmd+shift+e=equalize_splits
keybind = cmd+shift+f=toggle_split_zoom

# Font size
keybind = cmd+plus=increase_font_size:1
keybind = cmd+minus=decrease_font_size:1
keybind = cmd+zero=reset_font_size

# Tab management
keybind = cmd+shift+r=prompt_surface_title
keybind = cmd+shift+comma=reload_config

# Quick terminal
keybind = global:cmd+grave_accent=toggle_quick_terminal

# Terminal Inspector — debug key encoding issues
keybind = cmd+shift+i=inspector:toggle

# Jump between prompts in scrollback
keybind = cmd+up=jump_to_prompt:-1
keybind = cmd+down=jump_to_prompt:1

# Float window above IDE
keybind = cmd+shift+t=toggle_window_float_on_top

# Smart copy — copies if text selected, sends Ctrl+C interrupt if not
keybind = performable:cmd+c=copy_to_clipboard

# --- fixterms Workaround — EXPERIMENTAL ---
# Uncomment to test. Use Terminal Inspector (Cmd+Shift+I) to verify.
# If Ctrl+A still doesn't work after uncommenting, Claude Code's kitty
# protocol negotiation is overriding these — use Home/End instead.
# Track: https://github.com/anthropics/claude-code/issues/32087
#
# keybind = ctrl+a=text:\x01
# keybind = ctrl+e=text:\x05
# keybind = ctrl+u=text:\x15
# keybind = ctrl+k=text:\x0b
